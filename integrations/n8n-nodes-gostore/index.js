// n8n loads the node and credential classes via the "n8n" key in package.json.
// This file exists only so the package is require-able by tooling that expects
// a main entry point.
module.exports = {};
