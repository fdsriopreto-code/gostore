# n8n-workflow (dev-only, for types) pulls isolated-vm, a native addon that
# often fails to compile against newer Node headers. We only need the type
# declarations, never the runtime, so skip dependency build scripts.
ignore-scripts=true
audit=false
fund=false
